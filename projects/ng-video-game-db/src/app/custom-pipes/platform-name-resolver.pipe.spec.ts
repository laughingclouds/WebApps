import { PlatformNameResolverPipe } from './platform-name-resolver.pipe';

describe('PlatformNameResolverPipe', () => {
  it('create an instance', () => {
    const pipe = new PlatformNameResolverPipe();
    expect(pipe).toBeTruthy();
  });
});
